// ============ СКИНЫ ИГРОКОВ: ВРЕМЕННО ОТКЛЮЧЕНО ============
/* Skin Editor, Skins Browser и Avatar убраны целиком — автомодерация
   загружаемых картинок раз за разом пропускала неприемлемый контент,
   и решили выключить загрузку картинок игроками совсем, а не гоняться
   за очередным порогом нейросети.

   Ниже остались только два маршрута, которые всё ещё дёргает переживший
   отключение код:
   - /skin/my — game.js спрашивает у сервера собственный надетый скин при
     входе в игру;
   - /skins/many — skinAvatar.js (аватарки в шапке/профиле/списках друзей/
     таблице лидеров) и game.js (скины других игроков в комнате) запрашивают
     скины пачкой по никам.
   Оба ВСЕГДА отвечают «скина нет» — это тот же ответ, что раньше получал
   игрок без единого скина, поэтому вся отрисовка (и в игре, и в
   skinAvatar.js) естественно рисует обычный стандартный силуэт без каких-
   либо изменений в самих game.html/game.js/skinAvatar.js.

   Публикация, витрина и модерация убраны не только из меню, но и с
   сервера — прямой запрос в API тоже ничего не даст. Уже накопленные
   данные (data/userskins.json, data/skinimg/) не тронуты на случай, если
   функцию когда-нибудь вернут. */
'use strict';

function register(app) {
  app.get('/skin/my', (req, res) => { res.json({ img: '', kind: 'full' }); });
  app.get('/skins/many', (req, res) => { res.json({ skins: {} }); });
}

module.exports = { register, reload: () => {}, MINE_LIMIT: 0 };
